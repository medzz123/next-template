import { styled } from '@theme/config';

export const {{pascalCase name}}Container = styled('div', {
  backgroundColor: '$purple600',
});

export const {{pascalCase name}}Content = styled('div', {
  backgroundColor: '$purple600',
});
